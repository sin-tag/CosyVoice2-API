# App
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO
HOST=0.0.0.0
PORT=8010

# Database
DATABASE_URL=sqlite+aiosqlite:///./storage/voice_fast.db

# Auth
API_KEY=change-me-in-production

# IP Whitelist — comma-separated (empty = allow all)
ALLOWED_IPS=192.168.1.100

# Storage
VOICES_STORAGE_PATH=./storage/voices
HISTORY_STORAGE_PATH=./storage/history

# CORS
CORS_ORIGINS=["http://localhost:3000"]

# XTTS-v2 (17 languages, voice cloning, streaming, multi-GPU: cuda:0,cuda:1)
XTTS_ENABLED=true
XTTS_MODEL_PATH=tts_models/multilingual/multi-dataset/xtts_v2
XTTS_DEVICE=cuda:0

# Performance
MAX_CONCURRENT_GENERATIONS=2
GENERATION_TIMEOUT_SEC=120
MAX_TEXT_LENGTH=5000
MAX_CONCURRENT_REQUESTS=50
MAX_QUEUE_SIZE=100
